// import express from "express";
// import { keycloak } from "../keycloak";

// const router = express.Router();

// router.get("/get-user-data", keycloak.protect(), (req, res) => {
//   res.send("hello worsd");
// });
// // router.post("/send-data", keycloak.protect(), (req, res) => {
// //   console.log(`DEU BOM`);
// //   console.log(req.headers);
// //   res.json({});
// // });
// // router.put("/update-data", keycloak.protect(), () => {});
// // router.use(keycloak.middleware({ logout: "/logoff" }));

// export default router;
